"use client";
import Image from "next/image";

const RotateImage = () => {
  return (
    <div className="h-screen grid place-items-center">
      <Image src={""} alt="Person image" width={450} height={450} />
    </div>
  );
};

export default RotateImage;
